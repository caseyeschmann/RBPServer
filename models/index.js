const Course = require('./course');
const Employee = require('./employee');
// const Order = require('./order');
const Student = require('./student');

// ensure to add in order
module.exports = {Course, Employee, Student};
